# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local environment overrides (see .env.example)
.env
.env.*
!.env.example
# VITE_ vars end up in the public bundle, so the production API URL is not a
# secret and belongs in the repo — otherwise a fresh checkout builds against localhost.
!.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
